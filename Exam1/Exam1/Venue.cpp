#include <iostream>
#include <ctime>
#include <string>
#include "Venue.h"

void Venue::addEvent(int time, string name) { }

Event Venue::findEvent(int time)
{
	return Event(); // Able to find the event based on the Time
}

Event Venue::findEvent(string name)
{
	return Event(); // Searching for event by using the Name
}

